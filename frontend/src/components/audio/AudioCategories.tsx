import Image from 'next/image'

interface AudioCategoriesProps {
  categories: { label: string; imageUrl: string }[]
  title: string
}

export function AudioCategoriesProps({
  categories,
  title,
}: AudioCategoriesProps) {
  return <div></div>
}
